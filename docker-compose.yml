services:
  redis:
    image: redis:7-alpine
    container_name: fl-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  server:
    build:
      context: .
      dockerfile: Dockerfile.server
    container_name: fl-server
    env_file:
      - examples/server.env.example
    environment:
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      MIN_CLIENTS: "3"
      MODEL_DIR: /app/models
      ARTIFACT_DIR: /app/models
      LOG_DIR: /app/logs
      TRAINING_LOG_PATH: /app/logs/server-training.log
      MODEL_API_LOG_PATH: /app/logs/server-model-api.log
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "45678:45678"
    volumes:
      - models_data:/app/models
      - logs_data:/app/logs
      - ./data:/app/data:ro

  model-api:
    build:
      context: .
      dockerfile: Dockerfile.server
    command: /app/docker/model-api-entrypoint.sh
    container_name: fl-model-api
    env_file:
      - examples/server.env.example
    environment:
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      MIN_CLIENTS: "3"
      MODEL_DIR: /app/models
      ARTIFACT_DIR: /app/models
      LOG_DIR: /app/logs
      TRAINING_LOG_PATH: /app/logs/server-training.log
      MODEL_API_LOG_PATH: /app/logs/server-model-api.log
      MODEL_API_HOST: "0.0.0.0"
      MODEL_API_PORT: "8000"
    depends_on:
      redis:
        condition: service_healthy
      server:
        condition: service_started
    ports:
      - "8000:8000"
    volumes:
      - models_data:/app/models
      - logs_data:/app/logs
      - ./data:/app/data:ro

  client1:
    build:
      context: .
      dockerfile: Dockerfile.client
    container_name: fl-client-1
    env_file:
      - examples/client1.env.example
    environment:
      CLIENT_SERVER_ADDRESS: server:45678
      CLIENT_CONTROL_PORT: "8011"
      SERVER_CONTROL_URL: http://model-api:8000
    depends_on:
      server:
        condition: service_started
    ports:
      - "8011:8011"
    volumes:
      - ./data:/app/data:ro

  client2:
    build:
      context: .
      dockerfile: Dockerfile.client
    container_name: fl-client-2
    env_file:
      - examples/client2.env.example
    environment:
      CLIENT_SERVER_ADDRESS: server:45678
      CLIENT_CONTROL_PORT: "8012"
      SERVER_CONTROL_URL: http://model-api:8000
    depends_on:
      server:
        condition: service_started
    ports:
      - "8012:8012"
    volumes:
      - ./data:/app/data:ro

  client3:
    build:
      context: .
      dockerfile: Dockerfile.client
    container_name: fl-client-3
    env_file:
      - examples/client3.env.example
    environment:
      CLIENT_SERVER_ADDRESS: server:45678
      CLIENT_CONTROL_PORT: "8013"
      SERVER_CONTROL_URL: http://model-api:8000
    depends_on:
      server:
        condition: service_started
    ports:
      - "8013:8013"
    volumes:
      - ./data:/app/data:ro

volumes:
  models_data:
  logs_data:
  redis_data:
